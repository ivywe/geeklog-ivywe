{# begin {templatelocation} #}

<div uk-lightbox>
	<a href="{filename_orig}">
		<img src="{filename}" alt="{media_title}" class="uk-container-center uk-overlay-scale {class}" alt="{media_title}"{xhtml}>
	</a>
	<div>
		<p class="uk-margin-remove uk-text-bold">{media_title}</p>
		<p class="uk-margin-remove">{media_desc}</p>
	</div>
</div>

{# end {templatelocation} #}
